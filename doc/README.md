# MrMustard Documentation

Documentation for MrMustard lives in this directory.

To build the documentation locally:

1. Install the doc dependencies: `uv sync --group doc`
2. Run `make html` from this directory.
3. Open `_build/html/index.html` in your browser.
